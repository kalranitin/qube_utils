apiVersion: v1
kind: Service
metadata:
  labels:
    app: %appName%
  name: %appName%-service
  namespace: %namespace%
spec:
  ports: (( grab ports.service_ports ))
  imagePullSecrets: 
    - name: qubeship-partner-registry-key
  selector:
    app: %appName%
  type: ClusterIP
